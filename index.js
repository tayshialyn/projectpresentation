// stores our application logic
var options = {
    strings: ['Welcome to Project Destiny!', 'Ready to Explore?'],
    typeSpeed: 100,
    backSpeed:100,
    loop: false
};

var typed = new Typed('.typing', options);